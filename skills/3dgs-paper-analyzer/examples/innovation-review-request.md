使用 `3dgs-paper-analyzer` 的 `innovation-review` 模式，对 retrieval manifest 中的 P001 做创新主张、实验支撑、相似论文和改进方向分析。

要求输出：

- `P001.md`
- `P001.json`
- `P001.innovation-review.json`
- 可选 `P001.html`

标准 `P001.json` 必须继续符合 `schemas/paper-analysis.schema.json`，并在 `extensions.innovation_review` 中指向扩展 JSON。
